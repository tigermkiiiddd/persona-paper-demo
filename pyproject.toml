[project]
name = "persona-deduction-theater"
version = "0.1.0"
description = "人格演绎剧场 - Persona Deduction Theater"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "openai>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
]
